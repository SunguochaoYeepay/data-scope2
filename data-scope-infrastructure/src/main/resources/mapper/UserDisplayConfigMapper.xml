<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datascope.infrastructure.mybatis.mapper.UserDisplayConfigMapper">

    <resultMap id="BaseResultMap" type="com.datascope.domain.query.entity.UserDisplayConfig">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="data_source_id" property="dataSourceId"/>
        <result column="table_name" property="tableName"/>
        <result column="column_name" property="columnName"/>
        <result column="display_name" property="displayName"/>
        <result column="width" property="width"/>
        <result column="align" property="align"/>
        <result column="fixed" property="fixed"/>
        <result column="visible" property="visible"/>
        <result column="display_order" property="displayOrder"/>
        <result column="is_query_condition" property="isQueryCondition"/>
        <result column="is_required" property="isRequired"/>
        <result column="default_value" property="defaultValue"/>
        <result column="is_advanced_condition" property="isAdvancedCondition"/>
        <result column="mask_type" property="maskType"/>
        <result column="mask_config" property="maskConfig"/>
        <result column="usage_count" property="usageCount"/>
        <result column="last_used_at" property="lastUsedAt"/>
        <result column="nonce" property="nonce"/>
        <result column="created_at" property="createdAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="modified_at" property="modifiedAt"/>
        <result column="modified_by" property="modifiedBy"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, data_source_id, table_name, column_name, display_name, width, align, fixed,
        visible, display_order, is_query_condition, is_required, default_value, is_advanced_condition,
        mask_type, mask_config, usage_count, last_used_at, nonce, created_at, created_by,
        modified_at, modified_by
    </sql>

    <insert id="insert" parameterType="com.datascope.domain.query.entity.UserDisplayConfig">
        INSERT INTO tbl_user_display_config (
            <include refid="Base_Column_List"/>
        ) VALUES (
            #{id}, #{userId}, #{dataSourceId}, #{tableName}, #{columnName}, #{displayName},
            #{width}, #{align}, #{fixed}, #{visible}, #{displayOrder}, #{isQueryCondition},
            #{isRequired}, #{defaultValue}, #{isAdvancedCondition}, #{maskType}, #{maskConfig},
            #{usageCount}, #{lastUsedAt}, #{nonce}, #{createdAt}, #{createdBy}, #{modifiedAt},
            #{modifiedBy}
        )
    </insert>

    <update id="update" parameterType="com.datascope.domain.query.entity.UserDisplayConfig">
        UPDATE tbl_user_display_config
        SET user_id = #{userId},
            data_source_id = #{dataSourceId},
            table_name = #{tableName},
            column_name = #{columnName},
            display_name = #{displayName},
            width = #{width},
            align = #{align},
            fixed = #{fixed},
            visible = #{visible},
            display_order = #{displayOrder},
            is_query_condition = #{isQueryCondition},
            is_required = #{isRequired},
            default_value = #{defaultValue},
            is_advanced_condition = #{isAdvancedCondition},
            mask_type = #{maskType},
            mask_config = #{maskConfig},
            usage_count = #{usageCount},
            last_used_at = #{lastUsedAt},
            nonce = nonce + 1,
            modified_at = #{modifiedAt},
            modified_by = #{modifiedBy}
        WHERE id = #{id} AND nonce = #{nonce}
    </update>

    <delete id="deleteById">
        DELETE FROM tbl_user_display_config WHERE id = #{id}
    </delete>

    <delete id="deleteByTableName">
        DELETE FROM tbl_user_display_config
        WHERE user_id = #{userId}
        AND data_source_id = #{dataSourceId}
        AND table_name = #{tableName}
    </delete>

    <update id="updateUsage">
        UPDATE tbl_user_display_config
        SET usage_count = usage_count + 1,
            last_used_at = UNIX_TIMESTAMP() * 1000,
            nonce = nonce + 1
        WHERE id = #{id}
    </update>

    <select id="findByTableName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_user_display_config
        WHERE user_id = #{userId}
        AND data_source_id = #{dataSourceId}
        AND table_name = #{tableName}
        ORDER BY display_order
    </select>

    <select id="findByDataSourceId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_user_display_config
        WHERE user_id = #{userId}
        AND data_source_id = #{dataSourceId}
        ORDER BY table_name, display_order
    </select>

    <select id="findMostUsed" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_user_display_config
        WHERE user_id = #{userId}
        AND data_source_id = #{dataSourceId}
        ORDER BY usage_count DESC, last_used_at DESC
        LIMIT #{limit}
    </select>

    <select id="findRecommended" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_user_display_config
        WHERE data_source_id = #{dataSourceId}
        AND table_name = #{tableName}
        AND user_id != #{userId}
        ORDER BY usage_count DESC
        LIMIT 10
    </select>

</mapper>