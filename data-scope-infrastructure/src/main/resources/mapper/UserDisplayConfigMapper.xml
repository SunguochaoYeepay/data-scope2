<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datascope.infrastructure.mybatis.mapper.UserDisplayConfigMapper">

    <insert id="insert" parameterType="com.datascope.domain.query.entity.UserDisplayConfig">
        INSERT INTO user_display_config (id, user_id, data_source_id, table_name, column_name, display_name,
                                         width, align, column_fixed, visible, order_num, sortable,
                                         searchable, required, mask_type, mask_config, usage_count,
                                         last_used_at, created_by, created_time, updated_by, updated_time
        ) VALUES (
            #{id}, #{userId}, #{dataSourceId}, #{tableName}, #{columnName}, #{displayName},
            #{width}, #{align}, #{fixed}, #{visible}, #{order}, #{sortable},
            #{searchable}, #{required}, #{maskType}, #{maskConfig}, #{usageCount},
            #{lastUsedAt}, #{createdBy}, #{createdTime}, #{updatedBy}, #{updatedTime}
        )
    </insert>

    <select id="selectById" parameterType="java.lang.String"
            resultType="com.datascope.domain.query.entity.UserDisplayConfig">
        SELECT *
        FROM user_display_config
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" parameterType="java.lang.String"
            resultType="com.datascope.domain.query.entity.UserDisplayConfig">
        SELECT *
        FROM user_display_config
        WHERE user_id = #{userId}
    </select>

    <delete id="deleteById" parameterType="java.lang.String">
        DELETE
        FROM user_display_config
        WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="java.lang.String">
        DELETE
        FROM user_display_config
        WHERE user_id = #{userId}
    </delete>

    <update id="update" parameterType="com.datascope.domain.query.entity.UserDisplayConfig">
        UPDATE user_display_config
        SET user_id = #{userId},
            data_source_id = #{dataSourceId},
            table_name = #{tableName},
            column_name = #{columnName},
            display_name = #{displayName},
            width = #{width},
            align = #{align},
            column_fixed = #{fixed},
            visible = #{visible},
            order_num    = #{order},
            sortable = #{sortable},
            searchable = #{searchable},
            required = #{required},
            mask_type = #{maskType},
            mask_config = #{maskConfig},
            usage_count = #{usageCount},
            last_used_at = #{lastUsedAt},
            created_by   = #{createdBy},
            created_time = #{createdTime},
            updated_by   = #{updatedBy},
            updated_time = #{updatedTime}
        WHERE id = #{id}
    </update>

    <select id="selectByUserIdAndDataSourceId" resultType="com.datascope.domain.query.entity.UserDisplayConfig">
        SELECT *
        FROM user_display_config
        WHERE user_id = #{userId} AND data_source_id = #{dataSourceId}
    </select>

    <select id="selectByUserIdAndDataSourceIdAndTableName"
            resultType="com.datascope.domain.query.entity.UserDisplayConfig">
        SELECT *
        FROM user_display_config
        WHERE user_id = #{userId}
          AND data_source_id = #{dataSourceId}
          AND table_name = #{tableName}
    </select>

    <select id="selectByUserIdAndDataSourceIdAndTableNameAndColumnName"
            resultType="com.datascope.domain.query.entity.UserDisplayConfig">
        SELECT *
        FROM user_display_config
        WHERE user_id = #{userId}
          AND data_source_id = #{dataSourceId}
          AND table_name = #{tableName}
          AND column_name = #{columnName}
    </select>

    <delete id="deleteByUserIdAndDataSourceId">
        DELETE
        FROM user_display_config
        WHERE user_id = #{userId}
          AND data_source_id = #{dataSourceId}
    </delete>

    <delete id="deleteByUserIdAndDataSourceIdAndTableName">
        DELETE
        FROM user_display_config
        WHERE user_id = #{userId}
          AND data_source_id = #{dataSourceId}
          AND table_name = #{tableName}
    </delete>

    <insert id="copyConfigs">
        INSERT INTO user_display_config (id, user_id, data_source_id, table_name, column_name, display_name,
                                         width, align, column_fixed, visible, order_num, sortable,
                                         searchable, required, mask_type, mask_config, usage_count,
                                         last_used_at, created_by, created_time, updated_by, updated_time)
        SELECT UUID(),
               #{toUserId},
               data_source_id,
               table_name,
               column_name,
               display_name,
               width,
               align,
               column_fixed,
               visible,
               order_num,
               sortable,
               searchable,
               required,
               mask_type,
               mask_config,
               0,
               NULL,
               created_by,
               created_time,
               updated_by,
               updated_time
        FROM user_display_config
        WHERE user_id = #{fromUserId}
    </insert>
</mapper>
