<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datascope.infrastructure.mybatis.mapper.DataSourceMapper">

    <resultMap id="BaseResultMap" type="com.datascope.domain.datasource.entity.DataSource">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="host" property="host"/>
        <result column="port" property="port"/>
        <result column="database_name" property="database"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="salt" property="salt"/>
        <result column="status" property="status"/>
        <result column="last_sync_at" property="lastSyncAt"/>
        <result column="last_sync_status" property="lastSyncStatus"/>
        <result column="last_sync_message" property="lastSyncMessage"/>
        <result column="remark" property="remark"/>
        <result column="nonce" property="nonce"/>
        <result column="created_at" property="createdAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="modified_at" property="modifiedAt"/>
        <result column="modified_by" property="modifiedBy"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, type, host, port, database_name, username, password, salt,
        status, last_sync_at, last_sync_status, last_sync_message, remark,
        nonce, created_at, created_by, modified_at, modified_by
    </sql>

    <insert id="insert" parameterType="com.datascope.domain.datasource.entity.DataSource">
        INSERT INTO tbl_data_source (
            <include refid="Base_Column_List"/>
        ) VALUES (
            #{id}, #{name}, #{type}, #{host}, #{port}, #{database}, #{username}, #{password}, #{salt},
            #{status}, #{lastSyncAt}, #{lastSyncStatus}, #{lastSyncMessage}, #{remark},
            #{nonce}, #{createdAt}, #{createdBy}, #{modifiedAt}, #{modifiedBy}
        )
    </insert>

    <update id="update" parameterType="com.datascope.domain.datasource.entity.DataSource">
        UPDATE tbl_data_source
        SET name = #{name},
            type = #{type},
            host = #{host},
            port = #{port},
            database_name = #{database},
            username = #{username},
            password = #{password},
            salt = #{salt},
            status = #{status},
            last_sync_at = #{lastSyncAt},
            last_sync_status = #{lastSyncStatus},
            last_sync_message = #{lastSyncMessage},
            remark = #{remark},
            nonce = nonce + 1,
            modified_at = #{modifiedAt},
            modified_by = #{modifiedBy}
        WHERE id = #{id} AND nonce = #{nonce}
    </update>

    <delete id="deleteById" parameterType="string">
        DELETE FROM tbl_data_source WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM tbl_data_source WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectById" parameterType="string" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE id = #{id}
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
    </select>

    <select id="selectByName" parameterType="string" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE name = #{name}
    </select>

    <select id="existsByName" parameterType="string" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM tbl_data_source WHERE name = #{name})
    </select>

    <select id="selectByType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE type = #{type}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE status = #{status}
    </select>

    <select id="selectByLastSyncStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE last_sync_status = #{syncStatus}
    </select>

    <select id="selectByNameLike" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE name LIKE CONCAT('%', #{nameLike}, '%')
    </select>

    <select id="selectByHost" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE host = #{host}
    </select>

    <select id="selectByDatabase" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE database_name = #{database}
    </select>

    <select id="selectByTypeAndStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tbl_data_source
        WHERE type = #{type} AND status = #{status}
    </select>

</mapper>